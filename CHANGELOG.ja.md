# 変更履歴

[English version](CHANGELOG.md)

## v0.10.0

**最下段にフォルダタブができました。何度も戻る場所が1クリックになります——ツリーには絶対に出てこないフォルダも含めて。**

### フォルダタブ（Excelのシートタブ風）

- ウィンドウの最下段に横1列のタブが出ます。1タブ＝1フォルダで、押すとツリーがそこへ移ります。
- 狙いは「他の方法では辿り着けないフォルダ」です＝`~/.claude`、ワークスペースの外にある `memory/`、worktreeレーン。hidden の除外は**フォルダの子の名前を見て**弾いているので、hidden なフォルダでも「今見ているフォルダ」としてなら普通に開けます——タブはまさにそれをやる道具です。
- **足し方は3つ。** ツリーのフォルダを右クリック →**タブで開く**（タブにしたいフォルダは、たいてい今ツリーで見ているフォルダです。そこに入口が無かったことが問題の本体でした）。タブの並びの末尾にある`＋`を押すと、今見ているフォルダがその場でタブになります。他の足し方は`＋`の**右クリック**へ＝ワークスペースの隣で見つかった**worktreeレーン**、今のフォルダ、**ダイアログで選んだ任意のフォルダ**。レーンは開くたびに実測するので、撤収したレーンは自然に消え、新しいレーンは何も登録しなくても出てきます。レーンの検出に失敗しても他の項目は必ず出します（レーンはおまけで、本体ではないので）。
- `＋`は**最後のタブのすぐ隣**に置いてあります（右端固定ではありません）。タブが1枚の時に右端へ固定すると、タブと`＋`の間に画面幅ぶんの空白が空いて「タブを増やすボタン」に見えません——実際にそれで増やし方が見つかりませんでした。左クリックは即追加で、メニューは開きません（メニューを開くボタンは「選ばせるボタン」に見え、「増やすボタン」には見えないためです）。
- 既にタブがあるフォルダを足そうとした時は2枚目を作らず、そのタブへ移って**一瞬光らせます**。増えないぶん、押した操作が空振りに見えないようにするためです。
- **タブごとに続きを覚えます**＝展開していたフォルダ・選んでいたファイル（プレビューごと戻ります）・ツリーのスクロール位置。切り替えて戻った時に、畳まれたルートではなく離れた場所から再開できます。
- タブを右クリック: **名前を変更**（表示名だけ・パスは動かしません）／**パスをコピー**／**タブを閉じる**。`Ctrl+Tab`・`Ctrl+Shift+Tab`で隣、`Ctrl+1`〜`Ctrl+9`でn枚目へ。
- 1枚目はワークスペースで**閉じられません**。パス欄でどこかへ出ていても`⌂`は必ずそこへ戻します。1枚目の表示名は今いるフォルダに追従するので、実際と違う場所を名乗ることはありません。
- **タブを切り替えてもドロップの投入先は動きません。** タブが動かすのは「見ている場所」だけです。ワークスペース外にいる間の`↗`の印と「投入先は `_inbox` のまま」の注記も、今までどおり効きます。
- **保存していない編集**があるままタブを押すと一度確認し、取り消したら切り替えごと中止します（他のファイルを開く時と同じ確認です）。
- 行き先が消えたタブ（撤収したレーン・消したフォルダ）は`⚠`を付けて**そのまま残します**。自動で消すと、WSLが一瞬途切れただけでタブが失われます＝閉じるのは操作で。起動時も同じで、⚠は残したまま**表示だけ**ワークスペースへ落とします。
- **タブを一度も読み込んでいない状態では、絶対に保存しません。** WSLが上がる前に開くとワークスペースのピッカーで止まり、タブは空のままです。そこで終了時に書き出すと、前の晩のタブが空で上書きされて復旧できません。歯止めは「今ある呼び出し元」ではなく保存処理そのものの中に置いてあります。
- **閉じるのを取り消したら、何一つ変わっていません。** 未保存の編集があるタブを閉じる時は先に確認しますが、その確認は**タブを外す前**に出します。後から訊くと、〈いいえ〉を押した時にはもうタブは消えていて、消えた状態で保存まで済んでいます。
- 切替の走行中にタブを閉じる操作は受け付けません。逆に、走行中に来た切替は捨てずに覚えて後で実行します（`Ctrl+Tab`の連打が1回ぶん無反応になりません）。
- パス欄の移動でも未保存を確認するようにしました。移動するとそのタブが覚えていた選択ファイルが外れるので、ツリーのどこにも無いファイルを編集し続ける状態になり、次にタブを往復した時点で書きかけが消えていました。

### パス欄

- **`表示`（Go）ボタンを外しました。** Enterで同じことができるので重複です。空いた幅はそのまま入力欄に回しました。
- **`▾`の履歴は残します。中身は履歴だけです。** タブと履歴は重複ではなく別物でした＝タブは「置いておくと決めた場所」、履歴は「さっき行った場所」。worktreeレーンはこのメニューから外し、`＋`側に一本化しました（同じものへの入口を2つ持たない）。末尾に「履歴を消す」、履歴が空の時はその旨を出します（空の箱を開かせない）。パス欄で`↓`キーでも開きます。
- 履歴に積むのは**パス欄で移動した時だけ**です。タブの切替では積みません——往復するタブがあると同じ2箇所で履歴が埋まり、戻る先が何も残らなくなるためです。上限は20件、保存データが壊れていれば空に落として`▾`自体は壊しません。
- Enter・`↑`・履歴の行は、新しいタブを作らず**今のタブのパスを書き換えます**（ブラウザのタブと同じ）——でないと`↑`を連打するたびにタブが1枚ずつ増えます。

### フォルダのダブルクリックでタブを開きます

- ツリーの**フォルダ**をダブルクリックすると、そのフォルダがタブで開きます（タブの用途に対する最短の操作）。以前は開閉のトグルが2回走って相殺されたうえ、ついでにエクスプローラーまで開いていました。処理は右クリックの**タブで開く**と同じものを通るので、既にタブがあれば増やさずそこへ移り、未保存の編集があれば同じように一度確認します。エクスプローラーで開きたい時は右クリックの**開く**／**エクスプローラーで表示**が残っています。
- ダブルクリックの2発目では開閉を動かしません（タブが開くまでの間にフォルダが一瞬開いて閉じる、を防ぎます）。
- **ファイル**のダブルクリックは今までどおり、既定のアプリで開きます。

### 実装

- タブの保存先は `config.json` ではなく `localStorage` です。パスはPCごとに違い、このリポジトリは公開しているので、配布物の既定値に個人のパスを焼き込むわけにはいきません。保存データが壊れていても起動を止めず、ワークスペースのタブ1枚に戻します。
- 展開の復元は、展開中フォルダの集合を空にするのではなく**入れ替え**ます。2秒ごとの差分適用とポーリングが今までと同じ状態を見て動き続けます（全消し再描画もスクロール位置の飛びもありません）。
- 選択の復元は、展開の読み込みが終わるのを待ってから当てます。フォルダを開くと子を非同期で読みに行くので、待たずに当てると深い階層の選択が黙って空振りします。待つ回数には上限を置いてあり、循環した symlink でも止まりません。
- 「今のフォルダをタブに」は、普通の重複判定（既にタブがあるか）を通せません。パス欄で移動するとアクティブタブのパスが書き換わる仕様なので、今見ているフォルダは**常にアクティブタブのフォルダ**＝素直に繋ぐと`＋`は毎回自分自身に一致して1枚も増えない無反応ボタンになります。そのため一致を見るのは「今のタブ以外」だけにしました（他所に同じ場所のタブがあればそこへ移り、無ければ今の場所を新しい1枚として残します）。
- フォルダ選択には `choose-folder` IPC を新設しました。`choose-root` は**流用していません**——あちらはワークスペースごと書き換えるので、「タブを足しただけで投入先が黙って動いた」だけは絶対に起こしてはならない事故です。

### 検査

- `test-watch.js` の起動経路テストが、タブの実装（`loadTabs` / `startingTab` / `saveTabs`）を実体で通るようになりました。1枚目が必ずワークスペースになること、行き先が消えたタブは⚠を残したまま表示だけ落ちること、生きているタブは正規化後のパスで復元されること（WSL形式のままでは `readDir` に届きません）、壊れた保存データでも起動すること、v0.10より前の `browseRoot` がタブへ一度だけ移行されること、を見ます。
- タブ操作そのもの（`captureTab` / `activateTab` / `closeTab` / `goHome` / `addTab` / `pinCurrentTab` / `stepTab` / `gotoPath`）と、タブを増やす動線（`＋`の左/右クリックの分岐・ツリーの**タブで開く**）も、偽のファイルシステムと偽のDOMの上で**実体を走らせます**。起動経路だけではこれらに一度も触れず、**上の2件の不具合はどちらもその死角から出ました**。検査が本当に届いているかは変異を仕込んで確かめています＝退避した展開を捨てる／閉じた後の添字補正を落とす／復元したアクティブ位置を落とす／破棄確認を削除の後ろへ動かす／保存の歯止めを外す／パス欄の確認を外す／走行中の切替指示を捨てる／走行中に閉じる／選択やスクロールの復元を飛ばす／`＋`をメニューに戻す／`＋`が自分のタブにも一致する（無反応ボタン）／ハイライトを消す／**タブで開く**をファイル行にも出す／タブ切替でも履歴に積む／履歴の上限を外す／フォルダのダブルクリックを消す／行の種類を取り違える／ダブルクリックが未保存の確認を素通りする——35通り全てでテストが落ちることを確認しました。
- `grabFn`（`app.js` から関数を切り出して実体で走らせるための道具）が、引数リストの後ろから本体を読むようになりました。以前は最初に見つけた `{` から数えていたので、`activateTab(i, { force = false } = {})` のような分割代入の既定引数を本体と誤認し、半分だけの関数を切り出して意味の読めない構文エラーで落ちるところでした。

---

## v0.9.0

**エージェントがどこを直したのかが分かります。記事を全部読み直さなくて済みます。**

### 差分（開いた時点 → 今）

- プレビューヘッダに**差分**ボタンが出ます。押すと、そのファイルを**開いた時点から今まで**に変わった行だけが1カラムで並びます（削除=赤・追加=緑）。
- 基準（ベースライン）は**開いた時点で固定**です。自動更新でファイルを読み直しても動かしません＝エージェントが5回書き換えても、その5回ぶんが**まとめて1画面**で見えます。1回ぶんの差分しか見えないと、結局どこが変わったか追えなくなるので、ここは意図して固定しています。
- **同じファイルをもう一度開いても基準は動きません。** ツリーの同じ行を再クリックした時、ダブルクリックで外部エディタに渡した時（ダブルクリックの前にクリックが2発飛びます）、`←` で離れて戻った時、受領フィードの行を押した時——どれも「開く」処理を通ります。ここで基準を取り直すと、`●` を見て「エディタで読もう」とした瞬間に、まだ見ていない書き換えが復元不能で消えます。据え置きは呼び出し側ではなく「開く」処理の中の1箇所で塞いでいます（入口が増えるたびに塞ぎ忘れるため）。
- 無変更行は前後3行だけ残して `… N行省略 …` に畳みます。変更点だけ確認するのが目的なので、無変更行を全部出したら意味がありません。
- 何か変わっていれば差分ボタン自体に `●` が付きます。押さなくても「何か動いた」と分かります（ツリーの未読と同じ作法）。
- **確認済み**を押すと基準が今の内容に進み、差分ビューを閉じます。以後は「確認済みにした時点 → 今」の差分になります。書き換わった範囲が広すぎて差分を打ち切った時も、そこから抜ける道は**確認済み**です（基準を今に進めれば、次の書き換えからまた差分が出ます）。
- 自分が入力モードで保存した変更は基準に取り込みます。自分の編集が混ざると、エージェントの修正だけを見たいという目的が濁るためです。ただし `●` が点いたまま入力モードに入ろうとした時は**一度確認します**——そのまま保存すると、まだ見ていない変更ごと基準が進んで二度と差分に出せなくなるためです。
- 対象はMarkdownとコード（画像・PDF・docxには出ません）。**入力モード中は出しません**——編集中バッファと外部変更の衝突は慎重に扱っている領域で、そこに差分を持ち込みません。差分ビューから入力モードに入ると差分は閉じます。差分ビューを開いたままファイルが4MBを超えて育った時も差分は畳みます（畳まないと、縮んで戻った瞬間に押していない差分ビューが復活します）。
- 差分表示中にエージェントが編集を巻き戻して差分が無くなったら、**本文に戻します**。「変更はありません」の画面に取り残されると、本文を読むのにもう一度ボタンを押すことになり「壊れた?」に見えます。ただし無言では戻さず、右上のトーストで1回だけ知らせます。
- **末尾の空行だけの差はノイズとして扱い、`●` を点けません。** 整形ツールやエディタが末尾改行を足し引きしただけで「文字が1つも無い赤い行」が1本出るのは、日本語記事の運用で普通に踏みます。落とすのは末尾だけ＝本文中の空行の増減（段落を割った／繋いだ）は今までどおり差分に出ます。
- 本文中の空行が増減した時は `（空行）` と置きます。赤や緑の無地の帯が1本出るだけでは、何が起きたのか読めないためです。

### 実装

- diffは**依存パッケージを増やさず**、行単位のLCSを自前で持ちました。日本語の長文Markdownが対象なので行単位で足ります（語単位のハイライトは入れていません）。
- 比べる前に先頭・末尾の一致部分を落とします。記事の一部だけ直された時に、実際に計算する行数が「違う範囲」まで縮みます。
- サイズガードを入れました。素朴なLCSは O(N×M) でメモリと時間が膨らむので、トリム後の行数の積が **4,000,000セル**（Int32Array で約16MB）を超えたら計算せず打ち切り、「差分が大きすぎるため表示できません」と出します。黙って固まるのが一番困るためです。
- ファイル本文は必ずエスケープしてから描画します（本文中のHTMLがそのまま解釈される経路を作らない）。

### 検査

- `test-diff.js` を追加し、`check.sh` から毎回回します。変更なし／追加のみ／削除のみ／置換／先頭挿入／末尾追記、無変更行の畳み方の境界、サイズガードの発動、空ファイルとの往復、改行コードの正規化に加えて、**「自動更新の経路が基準を動かしていないか」**をソース構造として縛っています（ここが緩むと、差分は出るのに1回ぶんしか見えないという静かな壊れ方をします）。
- 上に挙げた直しぶんも回帰テストにしました。同じファイルを続けて開いても基準が進まないこと（離れて戻れば取り直すこと・Windowsの大文字小文字違いでも同じファイルと見ること）、末尾の空行だけでは `●` が点かないこと、**本文中の空行の増減は引き続き差分に出ること**（ノイズ除去で消しすぎていないことの担保）、editable でなくなった時と差分が無くなった時に差分ビューを畳むこと、を見ます。
- しきい値は**ドキュメントとも突合します**（`check.sh` の 9)）。`DIFF_CONTEXT` と `DIFF_MAX_CELLS` は README と CHANGELOG に手で書き写されていて、テストは本体から読むぶん気づけません＝前後の行数を変えた瞬間にREADMEが静かに嘘になります。ドキュメントの腐りは動かしても分からないので、本体から数字を抜いて6箇所と突き合わせ、ズレたら配布前に落とします。

---

## v0.8.0

**インストーラができました。ターミナルなしで導入できます（Windows・macOS両方）。**

### 配布

- [Releases](https://github.com/tact0225/claude-work-desk/releases/latest) に **Windowsインストーラ（.exe）** と **macOSディスクイメージ（.dmg・Apple Silicon/Intel両対応のユニバーサル）** を置きました。ダウンロードしてダブルクリックするだけで入ります。git clone もターミナルも不要です。
- ビルドは GitHub Actions がタグごとに自動で行い、Releases に添付します（手作業のビルド・アップロードはありません＝リリース漏れが起きない仕組み）。
- コード署名はしていません（証明書が有料のため）。初回だけ Windows は SmartScreen の「詳細情報→実行」、macOS は「右クリック→開く」が必要です。手順はREADMEに書きました。
- アプリアイコンを作りました（トレイに落ちる矢印＝受け渡し窓）。
- 従来のターミナル導入（`sync_to_windows.sh` / `setup_mac.sh`）もそのまま使えます。

---

## v0.7.1

**受領フィード（_inboxの窓）の高さをドラッグで変えられます。**

- ツリーと受領フィードの境界線を上下にドラッグすると窓の高さが変わります。サイズは記憶され、次回起動でも保持されます（横のサイドバー幅と同じ流儀）。
- 最小60px・ツリーが潰れない範囲でクランプします。

---

## v0.7.0

**worktreeレーンがパス欄の▾から選べます。ターミナルでパスを探す往復がなくなりました。**

### パス欄

- 履歴ドロップダウン（▾）の最上段に「worktreeレーン」が出ます。ワークスペースの**隣**にある `<ワークスペース名>-<スラッグ>` フォルダのうち、`.git` が**ファイル**で中身の gitdir が `worktrees/` を指すもの（= linked worktree）だけを載せます。名前の前方一致だけだと無関係な隣のフォルダ（`claude-work-desk` 等）まで拾うので、中身まで見て判定します。クリックでそのレーンへ移動。
- ▾を開くたびに実測し直します＝撤収済みレーンは自然に消え、新しいレーンは次に開けば現れます。gitコマンドは呼びません＝WSL越し（`\\wsl.localhost\...`）でもそのまま動きます。
- レーンが1つも無いときは今までと同じ見た目です（セクション自体が出ません）。

---

## v0.6.0

**macOSで動くようになりました。**

### macOS対応

- `bash setup_mac.sh` の1本で、自己検査 → 依存インストール → デスクトップに「claude-work Desk.command」生成まで終わります。以後はダブルクリックで起動。再実行しても同じ結果（冪等）で、macOS以外で走らせたらその場で止まります。
- **MacにはWindows版のような「配布コピー」の工程がありません（公開しないという意味ではありません）。** Windows版が`%LOCALAPPDATA%`へコピーして使うのはWSLとWindowsでファイルシステムが分かれているからで、Macはcloneした場所がそのまま実行環境です＝**更新は`git pull`だけ**で反映されます。
- 既定フォントを**OSで分岐せず**フォールバックチェーンで解決します（Windows→macOSの順に並べ、その環境に無いものは自然に飛ばされる）。UIは`-apple-system`・ヒラギノ、等幅は`SF Mono`・`Menlo`まで伸ばしました。設定のフォント選択にもMacのフォントを追加。
- 設定の「既定」表記からOS固有のフォント名（Segoe UI / Consolas）を外しました（8言語）。既定が何になるかはOSが決めるので、名指しはむしろ嘘になります。
- ズーム（`Ctrl/Cmd`+ホイール、`+` / `-` / `0`）が**Cmdでも効きます**。保存と貼り付けは以前からCmd対応だったので、そこに揃えた形です。

### 検査

- `check.sh` をmacOSでも**本当に走る**ようにしました。`grep -P`はGNU拡張でBSD grepには無く、そのままだとMacでは検査自体が失敗して`|| true`に吸われ、**検査したつもりの無検査**になります。POSIXの`-E`だけで書き直しました（検出内容は変えていません）。
- 既定フォントのチェーン一致を検査に追加しました。`renderer/styles.css`の`:root`と`renderer/app.js`の`FALLBACK_*`は同じ文字列の二重管理で、起動時に後者が前者を上書きするため、**片方だけ直すと「設定を触るまで旧チェーン」**という気づけないズレになります。ズレたら`check.sh`が落ちます。

### ドキュメント

- README（日本語・英語とも）にmacOSのセットアップを追加し、`user-config.json`の置き場所をOS別に併記しました。「Windowsネイティブで動かす前提」という但し書きは、**WSLg特有のD&D問題**と**WSL越しに変更通知が来ない話**に切り分けて、Macには当てはまらない部分が分かるようにしています。

---

## v0.5.0

**ツリーが勝手に最新になります。F5を押す必要がなくなりました。**

### 自動更新

- ツリーと開いているプレビューを**2秒ごとに見直す**ので、エージェントが書いたものが勝手に出てきます。
- 行は**作り直さず差し替える**ので、スクロール位置・選択行・展開状態はそのまま動きません。変化のないフォルダはDOMを1つも触りません。
- 外部で書き換わったプレビューは、**スクロール位置を保ったまま**読み直します。
- 入力モード中は編集中の内容に**一切触りません**。代わりにタイトルで外部更新を知らせ、入力モードを抜けた時に読み直します。
- 最小化中はポーリングを止め、復帰した瞬間に1回走ります。

> **なぜポーリングなのか**: WSL越しにはOSの変更通知が来ません（`\\wsl.localhost\` に対する `fs.watch` はそもそも失敗します）。鮮度は2秒ごとのポーリングで担保しています＝即時ではなく数秒遅れです。F5か⟳で全体を明示的に更新できます。

### 新着ハイライト

- フォルダを右クリック →「**新着を表示**」で監視対象にします。いくつでも足せます。
- 以後に現れたファイルに印が付き、**クリックするまで消えません**（時間で薄れません）。再起動しても残ります。
- 監視中のフォルダは、直下に未読がある間フォルダ自体にも印が出るので、**折りたたんでいても気づけます**。
- **直下のみ・非再帰**です。`todo/` を監視しても `todo/done/` は含みません。中も見たければそのフォルダを個別に足してください。
- ワークスペース直下は監視できません。直下のファイルが1000を超えるフォルダも断ります。

### 壊れた時に黙らない

- サイドバー下部に**最終確認の時刻**が出ます。ワークスペースに届いていない間は「待機中」、続けて3回失敗すると赤くなって理由を表示します。クリックで今すぐ更新／再開。
- 次の待ち時間は `max(2000ms, 直前のスキャン × 20)` なので、重いフォルダを指定しても更新が遅くなるだけで、PCは重くなりません。
- フォルダを読めなかった回は**何も変更しません**。WSLが一瞬落ちただけで「全部消えた＝全部新着」と誤認しません。
- 未読の印を消すのは、**本当にファイルが消えた時だけ**（親が読めて、そこに無い）。読めなかっただけでは消しません。

### テスト

`test-watch.js` を追加し、`check.sh` の1ステップとして組み込みました。**24観点 → 87観点**。本物のIPCハンドラ、rendererの全起動経路、ポーリング層、そしてツリー差分の行の同一性（＝全消し再描画に戻すとビルドが落ちる）まで見ます。

---

## それ以前のリリース

公開コミットから再構成したものです。

| バージョン | 変更 |
| --- | --- |
| v0.4.3 | 公開版の既定から社内フォルダ名を除去 |
| v0.4.2 | Obsidian移行の注意書き／リンク検査を月次保全へ |
| v0.4.1 | READMEに「なぜ作ったか」を追加 |
| v0.4.0 | ドロップ先フォルダを設定で選べるように |
| v0.3.1 | 受領フィード不具合の修正／READMEにスクリーンショット9枚 |
| v0.3.0 | UI 8言語対応／英語READMEを前面に |
| v0.2.2 | プレビューのコードブロックを折り返し表示に |
| v0.1.0 | 初回公開 |
